import { StyleSheet } from "react-native";
import { colorSchemas, fontSizeSchemas } from "../../../themes/default";

export const styles = StyleSheet.create({
  baseStyle : {
    color : colorSchemas.zinc[100],
    fontSize : fontSizeSchemas.md,
    fontWeight : '400'
  }
})